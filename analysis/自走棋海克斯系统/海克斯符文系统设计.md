# 海克斯符文系统设计方案（Hextech Rune System）

> 红警3日冕自走棋（DSA）自定义玩法设计文档
> 参考原型：英雄联盟「斗魂竞技场 / 海克斯大乱斗」三选一强化符文（Augment）肉鸽事件
> 状态：设计定稿；模式配置、真实候选抽取、首批符文效果、事件 UI 与真实数据面板已实现，剩余符文与正式素材待继续开发

> 实现基准：以 `E:\Ra3_AutoChess\DSA_ggz` 当前代码为准。本文同时记录已实现现状与后续目标；标记为“待实现”的内容不得视为现有功能。

---

## 1. 背景与目标

借鉴英雄联盟斗魂竞技场的**三选一强化符文**肉鸽机制：每隔若干回合，每位玩家独立看到 3 个随机符文，选择 1 个获得，效果可持续整局、可叠加成长。本方案将该机制本地化到红警3日冕自走棋，形成全新的「海克斯符文」模式。

**核心目标：**

- 每名玩家在特定回合弹出**三选一**符文对话框，独立选择、互不影响
- 符文效果全部使用本图已有系统实现（AttributeModifier 增益、发钱、召唤等）
- 作为**新模式开关**接入现有游戏模式选择流程，房主可选开/关
- 符文池本地化设计，覆盖进攻 / 防御 / 经济 / 召唤 / 功能五大类

---

## 2. 核心机制总览（目标正式流程）

> 本节描述最终玩法目标。当前代码已经按“先判定全场稀有度 → 每名玩家独立筛池 → 无放回抽取 3 个不同符文”运行，并接入首批可实测符文；完整符文数量与专属图标仍待补齐，实际进度见第 6 章。

```
回合开始
  └─ RoundLuaManager._begin() 触发 HextechRune:OnRoundBegin(lvc)
       ├─ 模式开关已开启（房主选择 或 开启了随机箱子）？
       │    └─ 当前回合 ∈ 发放回合表（3/10/18，按次数 N 截取）？
       │         └─ 对每个存活玩家：
       │              ├─ 按稀有度概率抽 3 个符文（去重 + 每人独占 + 兵种标签）
       │              └─ 弹出三选一对话框（选项含兵种标签）
       └─ 玩家点击选择
            ├─ 记录符文 + 兵种到 PlayerOwnedRunes[playerIndex] / PlayerOwnedRuneIds[playerIndex]
            └─ 登记到回合回调 → 每回合出兵时按兵种对己方单位施加 buff
                 └─ 公共面板广播「玩家X获得了符文Y」
```

---

## 3. 触发时机（已确认）

本图科技自动解锁节奏（用户确认 + 脚本佐证）：

| 科技阶段 | 自动解锁回合 | 脚本触发条件（lvc） | 符文事件回合（解锁前一回合） |
| -------- | ------------ | ------------------- | ---------------------------- |
| T2（二本） | 第 6 回合 | NATURALUNLOCK__1：lvc ∈ [4,5] | **第 5 回合** |
| T3（三本） | 第 12 回合 | NATURALUNLOCK__2：lvc ∈ [4,11] | **第 11 回合** |
| T4（四本） | 第 19 回合 | NATURALUNLOCK__3：lvc ∈ [4,18] | **第 18 回合** |

- 每名玩家整局共经历 **N 次**三选一。入局前未勾选「开启随机箱子」时默认不启用；勾选时默认 3 次。房主仍可在模式菜单中手动改为 0/1/2/3 次
- 事件在**备战阶段**弹出（RoundLuaManager 回合开始回调内），不影响战斗
- 玩家死亡/掉线时跳过该玩家（按存活判断）
- 一局的海克斯**发放次数可调**：房主开局选择 0/1/2/3 次；若入局前勾选「开启随机箱子」且房主未手动设置，则默认选 **3**，否则默认 **不启用**。减少次数时**去掉后面的发放**：
  - 3 次 = 第 3、10、18 回合
  - 2 次 = 第 3、10 回合
  - 1 次 = 第 3 回合

---

### 3.1 准确回合时长

`huihe/start` 每回合先将 `start` 计时器设为 **60 秒**，然后按当前回合计数 `lvc` 追加时间，因此实际回合时长不是固定 90 秒。设当前回合数为 `n`：

- `LV2 = 0`（初始阶段）：`T(n) = 60 + ⌈n / 2⌉` 秒。`lvcadd` 每次增加 1 秒，同时令 `lvcsr += 2`，直到 `lvcsr >= lvc`。
- `LV2 = 1`（`T72` 或 `T82` 被摧毁后）：`T(n) = 60 + 5 × ⌈n / 4⌉` 秒。`lvcadd__1` 每次增加 5 秒，同时令 `lvcsr += 4`，直到 `lvcsr >= lvc`。

例如第 10 回合在初始阶段为 65 秒，`LV2 = 1` 时为 75 秒；第 20 回合分别为 70 秒和 85 秒，第 24 回合的 `LV2 = 1` 阶段才达到 90 秒。所以所有“每 N 回合”效果应记录目标回合数，不应用固定秒数换算。

---

## 4. 符文池设计（用户定稿）

### 4.0 发放节奏与稀有度概率

三档稀有度「彩 / 金 / 银」对应素材边框色（银 = `augmentcard_frame_silver`，金 = `augmentcard_frame_gold`，彩 = `augmentcard_frame_prismatic`，见 5.0.2）：

| 发放 | 回合 | 彩 / 金 / 银 概率 |
| ---- | ---- | ---------------- |
| 第 1 个海克斯 | 第 3 回合 | **5% / 40% / 55%** |
| 第 2 个海克斯 | 第 10 回合 | **10% / 45% / 45%** |
| 第 3 个海克斯 | 第 18 回合 | **10% / 45% / 45%** |

> 第 3 回合彩/金/银为 5% / 40% / 55%；第 10、18 回合为 10% / 45% / 45%。全场在同一次事件中共用抽出的阶级，每名玩家再按个人状态筛选该阶符文池。

**全局规则：**

- 每回合**出兵时**给予 buff（非仅选择时），海克斯默认**只作用于自己的单位**（个别"全体效果"符文除外）
- 通用带兵种符文会展开为**四个独立候选**：步兵 / 载具 / 飞机 / 海军。不同兵种版本可同时出现在一次三选一中，也可被同一玩家分别持有；个别符文可以限制当前开放的兵种版本（无限火力目前只开放飞机），禁海模式下移除海军版本
- 一局的海克斯**发放次数可调**（入局前未开启随机箱子时默认 0 次，开启时默认 3 次；2 次 = 第 3、10 回合；1 次 = 第 3 回合；减少时去掉后面的发放）
- **启用方式联动**：与「抽卡功能 / 开启随机箱子」同理——若玩家在游戏前选择了**「开启随机箱子」**，则**一同视为开启海克斯系统**

### 4.0.1 当前已实现符文完整数值（以项目代码为准）

当前共有 **37 个基础定义**：银 12、金 14、彩 11。银色大力、迅捷、巡猎、万用瞄准镜、星界躯体均展开为四个兵种版本；金色万用瞄准镜、超凡邪恶展开为四个兵种版本；彩色万用瞄准镜展开为四个兵种版本，无限火力目前仅展开飞机版本。先进火炮、出口内销、高平两用分别仅对盟军、帝国、苏联玩家入池；彩色海上霸主仅在启用海军时入池，现金奖励仅对苏联阵营玩家入池。因此非禁海模式银色为盟军/帝国/苏联玩家 25 个、神州玩家 24 个，彩色 14 个；金色为苏联玩家 20 个、其他阵营玩家 19 个。禁海模式下银色为盟军/帝国/苏联玩家 20 个、神州玩家 19 个，彩色 12 个；金色为苏联玩家 18 个、其他阵营玩家 17 个。

| 阶级 | 符文 | 游戏内描述 | 实际数值与执行方式 |
| ---- | ---- | ---------- | ------------------ |
| 银 | 大力（兵种） | `%s单位造成的伤害提高 50%。` | 动态创建 `DAMAGE_MULT = 1.5`，即该兵种伤害 ×1.5。每回合出兵后，按选择者在该具体单位类型上的 `UNITCOUNT` 配额分配战斗单位并施加。 |
| 银 | 迅捷（兵种） | `%s单位攻击速度提高 50%。` | 动态创建 `RATE_OF_FIRE = 1.5` 的 AttributeModifier；特殊充能/弹药武器的实际收益仍需实机验证。 |
| 银 | 巡猎（兵种） | `%s单位移动速度提高 40%。` | 动态创建 `SPEED = 1.4`，即该兵种移动速度 ×1.4，持续 `9999`；不修改攻速、转向、射程或弹道速度。每回合只作用于按选择者单位池数量分配到的对应兵种战斗单位。 |
| 银 | 万用瞄准镜（兵种） | `%s单位射程提高 15%。` | 动态创建 `RANGE = 1.15`，即该兵种射程 ×1.15。卡框颜色已经表达阶级，标题不再附加“·银”。 |
| 银 | 星界躯体（兵种） | `%s单位生命提高 90%，但造成的伤害降低 25%。` | 动态创建同一个 AttributeModifier：`HEALTH_MULT = 1.9`、`DAMAGE_MULT = 0.75`。沿用玩家单位池配额分配与新单位标记，只作用于该玩家选择之后新出场的对应兵种单位，不追溯强化场上旧单位。 |
| 银 | MCV | `获得一个非自身阵营的 MCV。` | 选择时读取 `g_PlayerSide[playerIndex]`，在盟军、苏联、帝国、神州四个阵营中排除玩家自身阵营，再随机一个阵营；在玩家基地生成对应的 `AlliedMCV / SovietMCV / JapanMCV / CelestialMCV`，单位归属选择者自己的队伍。抽到神州时仍遵循项目现有青龙核心舰/伏龙殿处理规则。 |
| 银 | 超级要塞 | `获得 1 个超级要塞核心。` | 在玩家基地生成归属该玩家的 `JapanGigaFortressShipEgg`。`UnitCreate.lua` 已为它注册 `UnitCountFunc`，8 帧后增加该玩家的超级要塞独立计数并删除场上核心，之后由空军 AI 出兵流程生成。自走棋内归类为空军，禁海模式仍可抽取。 |
| 银 | 五虎上将 | `每回合随机使最多 5 个己方步兵成为百夫长。` | 每次固定出兵登记完成后延迟 `45` 帧（3秒），只从该玩家本回合新登记、仍存活且尚无百夫长附着物的步兵中同步随机选择至多 5 个；少于 5 个则全部选择，为 0 不执行。通过对应战斗 AI 的 `playerspellbook` 调用原生 `Command_Celestial_CenturionUpgrade`，完整生成 `CelestialCenturionUpgradeObject`，保留百夫长数值、模型标记和光环视觉。同一单位只处理一次；多份符文每份增加 5 个名额。符文来源附着物会绕过地图 319 版本遗留的“第 9 回合后新百夫长可晋升千夫长”回调，始终只授予基础百夫长，不影响玩家正常神州协议的千夫长/万夫长流程。 |
| 银 | 质变 | `获得更高一阶的随机符文。` | 点击选择后立即从该玩家筛选后的金色池随机授予 1 个符文；奖励池沿用阵营、禁海、兵种及不可重复条件，并排除质变和赌怪以避免奖励链递归。质变本体可重复获得，不在海克斯总览面板显示，面板只记录实际获得的奖励符文。奖励结果会全图播报。 |
| 银 | 出口内销 | `你的太刀重巡洋舰单位会升级为一式磁轨炮重巡。` | 帝国限定且不可重复。`unitgetcountanddelet` 回收 `JapanAntiNavyShipTech3` 时，在写入单位池和抽卡获得播报前把计数槽替换为 `CelestialSeized_JapanAntiNavyShipTech3`。 |
| 银 | 高平两用 | `你的牛蛙战车升级为“偏流”两栖高射炮。` | 苏联限定且不可重复。回收 `SovietAntiAirShip` 时，在计数与播报前替换为 `VUAntiAirVehicleTech1`。 |
| 银 | 先进火炮 | `你的先锋武装炮艇机“升级”为AC130版本。` | 盟军限定且不可重复。回收玩家生产的 `AlliedGunshipAircraft` 时，在计数与播报前替换为隐藏单位 `AlliedAC130GunshipAircraft`；两者使用独立单位池槽位。 |
| 金 | 湮灭炸弹 | `获得 1 次湮灭炸弹部署次数。` | 选择时只累计部署次数，不立刻生成。只要次数大于 0，J 键提示下方持续显示“按'K'键部署湮灭炸弹”；每按一次 K 消耗 1 次，在抽卡空投使用的战场圆心 `(3547.06, 3055.49)` 生成归属使用者的 `japanomegaoblivionbomb`，Z 高度取 `T74/T84` 的平均地面高度。允许重复取得并累计次数。 |
| 金 | 穿云定海 | `获得 1 个摇光。` | 在 `Player_N_Start` 附近为选择者自己的队伍生成 `CelestialAdvanceAircraftTech4`，并标记为脚本生成单位，随后交给现有 `unitgetcountanddelet` 回收计数流程进入单位池。摇光“限造 3 个”改用独立的实际生产计数，因此这只赠送摇光不占生产额度。剩余生产额度统一限制在 `0～3`；回收赠送摇光不会增加额度，回收实际生产部分最多恢复到 3。 |
| 金 | 石油王 | `获得两个油井。` | 在选择者出生点附近生成两个归属该玩家队伍的 `oilderrick`。 |
| 金 | 买二送一 | `%s单位买二送一。` | 为每名玩家生成三选一事件时，就从其自身阵营的有效 `UNITLIST` 单位中独立随机确定并固定目标，悬浮详情会直接显示如“动员兵单位买二送一”；点击时不会重新随机。效果复用狂热武士的回收计数时机，每累计两个目标单位直接向 `UNITCOUNT` 和 `ANYUNITCOUNT` 赠送一个，不生成待回收实体，因此不会递归计数。 |
| 金 | 启动资金 | `立即获得 15000 资金。` | 选择时仅给选择者增加 15000；复用主动回收与青龙船自动回收的可靠执行方式：先用 `SetWorldBuilderThisPlayer(1)` 切换 WorldBuilder 执行上下文，执行 `PLAYER_GIVE_MONEY` 后恢复原上下文。不是持续收入，不影响队友。 |
| 金 | 现金奖励 | `获得现金奖励协议。` | 原生规则要求玩家拥有苏联基地车才能使用，因此正式候选池仅对苏联阵营玩家开放。`Purchase_PlayerTech_Soviet_ProductionKickbacks` 是协议树购买按钮，真正能力 ID 是 `SpecialPower_ProductionKickbacks`，底层科技为 `PlayerTech_Soviet_ProductionKickbacks`。地图开局仍对六名玩家分别设置 `Disabled`；选择后解除持有者科技锁、授予能力、设为 `Available` 并清零倒计时，下一帧重复一次。 |
| 金 | 安全感 | `获得 2 个埃奎斯护盾发生器。` | 复用交易市场的生成参数，在本方最前端存活防御塔后方的左右两侧各生成一个 `AlliedAegisLargeDefenseBase`，归属本方 AI。允许重复取得并重复生成；不写入 `g_BuyTowerId`，因此不占用市场的两个常规购买名额。 |
| 金 | 固若金汤 | `最前端防御塔生命值增加 25%。` | 自身唯一。简短文案不区分防线；实际选择时不判断禁海配置，同时查找陆地和海上两条防线各自第一座存活塔并强化。玩家 1～3陆塔顺序为 `T71→T74`、海塔为 `T71F→T73F`；玩家 4～6对应 `T81→T84`、`T81F→T83F`。队友可各持有一份；同一座塔多份以第一次强化前的基础最大生命直接相加，1/2/3份分别为 +25%/+50%/+75%，不进行复利乘算。实测陆塔原始最大生命：`T81=2813`、`T82=18000`、`T83=55000`、`T84=200000`；对侧对应塔预计相同，仍以运行时读取值为准。 |
| 金 | 破烂王 | `回收倍率提高 20%。` | `最终回收倍率 = 原回收倍率 ×1.2`，是乘算提高 20%，不是直接增加 20 个百分点。例如原回收率 70% 时变为 84%。只影响选择者。 |
| 金 | 万用瞄准镜（兵种） | `%s单位射程提高 25%。` | 动态创建 `RANGE = 1.25`，即该兵种射程 ×1.25。卡框颜色已经表达阶级，标题不再附加“·金”。 |
| 金 | 超凡邪恶（兵种） | `%s单位的生命和造成的伤害每回合提高3%。` | 按当前回合数动态生成 `HEALTH_MULT` 与 `DAMAGE_MULT`：加成为 `当前回合数 ×3%`，例如第 20 回合为生命、伤害各 +60%。同一玩家的同一兵种版本不可重复取得。只对选择之后的新出场单位生效，并在每次新出兵时按当时回合数计算。 |
| 金 | 灯火辉煌 | `在前线防御塔前方召唤 2 个中级天灯。` | 选择时查找玩家所属阵营当前最前端存活防御塔，在塔前左右两侧生成 2 个归属该玩家队伍的 `CelestialEngineerRepairDroneLv2`。 |
| 金 | 质变 | `获得更高一阶的随机符文。` | 点击选择后立即从该玩家筛选后的彩色池随机授予 1 个符文；筛池及播报规则与银色质变相同。质变本体可重复，不在海克斯总览面板显示。 |
| 金 | 炽燃利息 | `敌方每个死亡单位令你获得 6 资金。` | 玩家自身不可重复取得。仅统计已经登记到 `BattleUnitAssignments` 的战场单位，回合结算后的脚本清场不触发收益。`PlyrCivilian` 单位死亡时，P4～P6 中每名符文持有者各获得 6；`PlyrCreeps` 单位死亡时，P1～P3 中每名符文持有者各获得 6。无须判断击杀者；多人同时获得收益时，死亡单位头顶仅显示一次 `+6`。 |
| 彩 | 无限火力（飞机） | `飞机单位获得无限弹药。` | 作为带兵种符文记录为 `prismatic_infinite_ammo:aircraft`，但实际单位有白名单：阿波罗、阿瑞斯、凤凰、崇明、米格、苏霍伊、摇光巡天炮及代码中登记的同机型强化/尾焰变体。对按选择者飞机单位池数量分配到的白名单飞机，把武器槽 1～5写入 `100000` 弹药；不处理世纪轰炸机等轰炸机，也不重复处理原本无限弹药的天狗、心神。不复制空军元帅苏霍伊的无敌及其他属性 Buff。 |
| 彩 | 万用瞄准镜（兵种） | `%s单位射程提高 50%。` | 动态创建 `RANGE = 1.5`，即该兵种射程 ×1.5。卡框颜色已经表达阶级，标题不再附加“·彩”。 |
| 彩 | 全频段阻塞干扰 | `敌方全体单位射程降低 25%。` | 玩家自身不可重复取得（隐性属性，不写入游戏内简介）。选择时对玩家对应 AI 的敌对 AI 全部战斗单位加载 `RANGE = 0.75`、持续 `9999` 的 Modifier；固定出兵和“补充军队”后再次扫描，只处理尚未加载该玩家符文实例的新单位。同阵营队友可各持有一份，每名持有者使用独立实例标记并再次加载 ×0.75，按乘算叠加：1/2/3 份分别为原射程的 75%/56.25%/42.1875%。玩家 1～3 的敌方为 `PlyrCreeps/P[8]`，玩家 4～6 的敌方为 `PlyrCivilian/P[7]`。 |
| 彩 | 神圣干预 | `己方全体单位和建筑每 30 秒获得 3 秒铁幕。` | 玩家自身不可重复取得，队友叠加延长属于隐性属性，不写入游戏内简介。取得时立即触发一次，之后由全局调度器每 `450` 帧（30秒）触发；对玩家对应 AI 的全部单位和建筑加载 `AttributeModifier_IronCurtain`。基础时长为 `45` 帧（3秒）；同阵营每多一份增加基础时长的一半，半数帧向上取整为 `23` 帧，因此同阵营 1/2/3 份分别为 45/68/91 帧，即约 3/4.53/6.07 秒。玩家 1～3 对应 `PlyrCivilian/P[7]`，玩家 4～6 对应 `PlyrCreeps/P[8]`。 |
| 彩 | 五雷天罚 | `每 3 回合获得一次五雷天罚。` | 首发 `SpecialPower_CelestialPantaOrbitalStrike` 与 0cd `SpecialPower_CelestialOrbitalStrike0cd` 属于一套原生五连发流程。实测引擎可能只记录 0cd 输出段；首次检测到任一段触发后，只禁用首发入口，不立即禁用或上冷却给 0cd，使后续四次落雷继续由原生流程完成。以释放时回合数记录 `ReadyRound = lvc + 3`；到目标回合后先等待旧触发状态消失，再对两段执行倒计时清零和 `Available`。恢复时不重新 Grant，避免 0cd 历史触发立即重入冷却。 |
| 彩 | 磁暴突袭 | `每 3 回合获得一次磁暴突袭。` | 选择时立即授予并开放 `SpecialPower_SovietTeslaAirAssault`。该协议的 `PLAYER_TRIGGERED_SPECIAL_POWER` 在当前环境中不能稳定返回触发态，因此改以实际生成的 `SovietSurveyor / SovietSurveyor_Naval` 出生作为成功释放信号：产物出生时立刻禁用持有者技能并记录 `ReadyRound = lvc + 3`；到目标回合清零内部倒计时并重新开放。两种史普尼克勘查车对自走棋均无用途，已在全局出生回调中对所有人类玩家和 AI 立即删除。 |
| 彩 | 塔防专家 | `购买防御塔时无视数量限制，并且价格降低 50%。` | 自身唯一，仅影响选择者本人使用交易市场购买的四种防御塔。显示价、余额校验和实际扣款均为原价 ×0.5：护盾塔 5000、樱花井 5000、胡杨塔 6000、埃奎斯 7500。购买时跳过共享存活数量限制且不写入 `g_BuyTowerId`；其他玩家不受影响。额外埃奎斯左右交替并逐层外扩，减少重叠。 |
| 彩 | 终极刷新 | `第一和第二技能的使用次数各增加 1 次。` | 选择时给玩家第一、第二技能按钮的最大使用次数各 +1；若技能已经选择则立即刷新按钮数据，尚未选择的技能在之后创建按钮时补加。可重复获得并累计次数。 |
| 彩 | 赌怪 | `获得两个随机符文。` | 点击后独立抽取并授予 2 个随机符文；每个奖励的阶级概率为彩 5%、金 30%、银 65%。每次奖励均使用玩家自己的正式筛池，并排除质变、赌怪本体。赌怪自身不可重复，且不在海克斯总览面板显示；两个实际奖励分别进入持有列表并全图播报。 |
| 彩 | 海上霸主 | `获得奥林匹斯级航空母舰和千鸟特攻母舰各 1 个。` | 与穿云定海使用相同赠送流程：在玩家基地分别生成归属选择者队伍的 `AlliedGaintAirCraftCarrier_B` 与 `JapanYumiAircraftCarrier`，每次生成前分别标记为脚本赠送单位，再由现有回收系统收入玩家单位池。符文定义带 `RequiresSea = true`，`g_DisableSeaArmy == 1` 时在个人候选池筛选阶段直接排除。 |

**破烂王的实际回收率：**

| 经济倍率 | 项目基础回收率 | 取得破烂王后 |
| -------- | -------------- | ------------ |
| 0.5 | 90% | 108% |
| 1.0 | 70% | 84% |
| 1.5 | 50% | 60% |
| 2.0 | 35% | 42% |

非纯抽卡模式还会先计算队伍中的苏联“大生产”修正：0/1/2/3 层对应 ×1 / ×0.75 / ×0.70 / ×0.65，最后再乘破烂王的 ×1.2。例如正常经济倍率、1 层大生产时为 `70% ×0.75 ×1.2 = 63%`。纯抽卡模式不应用大生产削减。

**兵种版本与判重：**

- 兵种版本使用 `基础符文ID:unitType` 作为完整持有 ID，例如 `silver_strength:infantry` 和 `silver_strength:vehicle` 是两个不同符文。
- 同一轮三选一允许同时出现同名但兵种不同的符文；同一玩家也可以分别持有这些版本。
- 同一轮的三个候选始终无放回抽取。跨轮是否允许再次出现由 `HextechRune.NonRepeatableRuneIds` 独立控制；当前固若金汤、超凡邪恶、全频段阻塞干扰、神圣干预、炽燃利息、塔防专家、赌怪不可重复，其中超凡邪恶按兵种版本分别判重。湮灭炸弹允许重复获得并累计部署次数。
- 未列入不可重复表的符文可以跨轮再次出现；每次选择记录独立效果实例，使持续 Buff 能按实例分别加载。买二送一会保留多份独立目标/计数状态，破烂王每次取得累加 20% 回收倍率。
- 即使玩家当前还没有生产对应兵种，该兵种版本仍可进入候选池，以便作用于之后生产或刷新的单位。
- 禁海时不生成海军版本，其余三个兵种版本不受影响。

**兵种作用范围：**

| 兵种 | 当前过滤范围 |
| ---- | ------------ |
| 步兵 | `INFANTRY`；排除建筑、飞机、船只和残骸 |
| 载具 | `VEHICLE`、`HUGE_VEHICLE`；排除普通飞机、船只和残骸；额外包含双形态的天狗、VX、心神及其已登记强化/水面变体 |
| 飞机 | `AIRCRAFT`；排除建筑和残骸；额外包含天狗、VX、心神、海翼、超级要塞空中要塞/大头形态及其已登记变体 |
| 海军 | `SHIP`；排除建筑和残骸；额外包含双形态的海翼与超级要塞空中要塞/大头形态 |

双形态单位使用显式 `IncludeThing` 补充表，不依赖单位当前变形后的 category：天狗（`JapanAntiInfantryVehicle`）、VX（`JapanAntiAirVehicleTech1`）、心神（`JapanMissileMechaAdvanced`）同时属于载具和飞机；海翼（`JapanAntiAirShip`）、超级要塞空中要塞/大头形态（`JapanFortressShip` / `JapanGigaFortress_Land`）同时属于飞机和海军。其它单位只按引擎原生兵种 category 判定。

伤害、攻速、移速和射程类效果使用与缩小模式一致的 `9999` 持续时间。系统以战斗单位 `objectId` 保存“配额归属玩家 + 已施加的符文实例 ID”，并保留单位句柄识别 ID 复用；因此同一单位的同一持有实例只加载一次，可重复符文的新实例仍能独立生效。玩家在本回合中途选到新符文时，会给该玩家已登记且仍存活的单位补施加这个新实例，不会重复加载已有实例的 Modifier。

**混编战斗单位的玩家配额分配：**

- 实际战斗复制体统一属于 `PlyrCivilian`（玩家 1～3）或 `PlyrCreeps`（玩家 4～6），不能再用 `Player_1～6 + SAME_PLAYER` 判断原始归属。
- 系统对每一种 `UNITLIST[unitIndex]` 分别扫描场上实例，先排除已有有效标记的幸存单位，再按阵营内玩家顺序及 `UNITCOUNT[playerIndex][unitIndex]` 切分本次新单位。例如本次新增 10 只天狗，A 的单位池为 3、B 为 6、C 为 1，则 `[1..3]` 视为 A 的配额、`[4..9]` 视为 B 的配额、最后 1 只属于 C；具体是哪几只实体不重要。
- 同一玩家拥有的多个对应兵种符文可以叠加在自己的同一配额切片上；不同玩家的切片互不重叠。场上数量少于记录数量时截断，超过单位池总数的额外单位不处理。
- 兵种仍通过 `INFANTRY / VEHICLE / HUGE_VEHICLE / AIRCRAFT / SHIP` 查找表判断，因此单位池配额负责区分玩家，兵种过滤负责决定某个符文是否适用于该实体。
- 固定回合出兵与“补充军队”共用同一个新单位处理入口。“补充军队”原流程分两段生成，原有 `ShrinkMode_Trigger` 位于两段之间，因此在全部生成结束后延迟 1 tick 再补扫一次；单位标记保证前半段不会被重复加载。
- 符文效果层与 J 键排查阶段使用的 `_ALERT()` 调试输出均已移除；玩家选择提示、错误提示以及质变/赌怪获得奖励时的全图播报继续保留。

> 以下 4.1～4.3 为完整规划池。当前真正进入游戏候选池并拥有代码效果的符文，仅限 4.0.1 表中列出的 37 个基础定义；其余条目仍是待实现设计。

### 4.1 银色符文

| 符文名 | 兵种 | 效果 | 本地化实现 |
| ------ | ---- | ---- | ---------- |
| 大力 | 兵种 | 伤害 +50% | 动态创建 `DAMAGE_MULT = 1.5` 的 AttributeModifier |
| 迅捷 | 兵种 | 攻击速度 +50% | 动态创建 `RATE_OF_FIRE = 1.5` 的 AttributeModifier |
| 星界躯体 | 兵种 | 生命 +90%，伤害 -25% | `HEALTH_MULT = 1.9`、`DAMAGE_MULT = 0.75` |
| 存护 | 兵种 | 防御 buff | 防御/护甲类 AttributeModifier |
| 巡猎 | 兵种 | 移速 +40% | 动态创建 `SPEED = 1.4` 的 AttributeModifier |
| MCV | — | 获得一个**非自身阵营**的 MCV | 生成单位 |
| 超级要塞 | 空军 | 获得 1 个超级要塞核心 | 生成 `JapanGigaFortressShipEgg`，回收后进入空军 AI 队列；不受禁海影响 |
| 五虎上将 | 步兵 | 每回合随机使最多 5 个己方新出场步兵成为百夫长 | 延迟 3 秒后调用原生百夫长命令，保留完整附着物和视觉；不触发第 9 回合后的千夫长晋升 |
| 万用瞄准镜（银） | 兵种 | 射程 +15% | 动态创建 `RANGE = 1.15` 的 AttributeModifier；卡面标题只显示“万用瞄准镜” |
| 质变 | — | 获得 1 个随机金色符文 | 即时奖励入口，可重复；本体不进入总览面板，奖励结果全图播报 |
| 出口内销 | — | 太刀重巡升级为一式磁轨炮重巡 | 帝国限定；回收计数时替换为 `CelestialSeized_JapanAntiNavyShipTech3` |
| 高平两用 | — | 牛蛙战车升级为“偏流”两栖高射炮 | 苏联限定；回收计数时替换为 `VUAntiAirVehicleTech1` |
| 先进火炮 | — | 先锋武装炮艇机升级为AC130版本 | 盟军限定；回收计数时替换为 `AlliedAC130GunshipAircraft` |

### 4.2 金色符文

| 符文名 | 兵种 | 效果 | 本地化实现 |
| ------ | ---- | ---- | ---------- |
| 湮灭炸弹 | — | 获得 1 次湮灭炸弹部署次数 | 次数可累计；按 K 后消耗一次并在空投圆心生成玩家所属的 `japanomegaoblivionbomb` 建筑 |
| 买二送一 | — | 随机一个自身阵营单位**买二送一** | 复用现有造二送一机制（参考狂热武士） |
| 穿云定海 | — | 获得 1 个摇光 | 在玩家基地生成后由系统回收进单位池 |
| 固若金汤 | — | 己方陆地及海上当前仍存活的最前端防御塔生命 +25% | 两条防线分别查找；队友多份按第一次记录的基础最大生命线性相加，不判断禁海模式 |
| 石油王 | — | 获得两个油井 | 生成油井建筑 |
| 启动资金 | — | 获得 15000 资金 | `ExecuteAction('PLAYER_GIVE_MONEY', ...)` |
| 现金奖励 | 苏联限定 | 获得现金奖励协议 | 仅苏联阵营玩家进入候选池；选择后解锁 `PlayerTech_Soviet_ProductionKickbacks`，授予 `SpecialPower_ProductionKickbacks`，同帧及下一帧分别为持有者单独解禁并清零技能倒计时 |
| 安全感 | — | 获得 2 个埃奎斯护盾发生器 | 左右各生成一个，不占市场购买名额，可重复获得 |
| 破烂王 | — | 最终回收倍率 ×1.2 | 修改 `GetRecycleRate` 返回值 |
| 万用瞄准镜（金） | 兵种 | 射程 +25% | 动态创建 `RANGE = 1.25` 的 AttributeModifier；卡面标题只显示“万用瞄准镜” |
| 超凡邪恶 | 兵种 | 生命和伤害每回合 +2% | 新出兵按当前回合数与相同兵种持有份数动态计算 |
| 灯火辉煌 | — | 在前线塔前召唤 2 个中级天灯 | 生成 `CelestialEngineerRepairDroneLv2`，归属选择者 |
| 质变 | — | 获得 1 个随机彩色符文 | 即时奖励入口，可重复；本体不进入总览面板，奖励结果全图播报 |
| 炽燃利息 | — | 敌方每个死亡单位提供 6 资金 | 仅结算已登记战斗单位；自身唯一 |

### 4.3 彩色（棱彩）符文

| 符文名 | 兵种 | 效果 | 本地化实现 |
| ------ | ---- | ---- | ---------- |
| 终极刷新 | — | 第一、第二技能使用次数各 +1 | 已选择技能立即更新，未选择技能在创建时补加；可重复累计 |
| 神圣干预 | — | 对应己方 AI 的全体单位和建筑每 30 秒获得 3 秒铁幕；同阵营额外每份约 +1.53 秒 | `AttributeModifier_IronCurtain` 定时施加；自身唯一 |
| 五雷天罚 | — | 每 3 回合获得一次五雷天罚 | 首次触发只关闭首发入口，保留 0cd 完成原生五连发；第 3 回合清零两段内部倒计时并恢复 |
| 塔防专家 | — | 市场防御塔无数量限制且价格 -50% | 玩家级市场规则；购买塔不占共享名额；自身唯一 |
| 无限火力 | 飞机（当前仅开放此版本） | 武器槽 1～5 的弹药设为 100000，参考空军元帅苏霍伊 | `ObjectSetWeaponSetUpdateWeaponCurrentAmmoCount`；以后可通过 `UnitTypes` 添加其他兵种版本 |
| 全频段阻塞干扰 | — | 敌方全体单位射程 -25% | `RANGE = 0.75` 作用于敌方 AI；自身唯一 |
| 万用瞄准镜（彩） | 兵种 | 射程 +50% | 动态创建 `RANGE = 1.5` 的 AttributeModifier；卡面标题只显示“万用瞄准镜” |
| 赌怪 | — | 获得 2 个随机符文 | 每个奖励独立按彩 5% / 金 30% / 银 65% 抽阶级；自身唯一，本体不进入总览面板 |
| 海上霸主 | — | 获得奥林匹斯级航空母舰和千鸟特攻母舰各 1 个 | 生成 `AlliedGaintAirCraftCarrier_B` 与 `JapanYumiAircraftCarrier` 后回收进单位池；禁海时不入池 |

> 万用瞄准镜比较特殊：它跨彩/金/银三个稀有度存在，三档数值不同；每一档又按步兵/载具/飞机/海军拆成独立候选。

---

## 5. 技术约束与自定义图片机制

### 5.0 本图 Lua 开发约束（重要）

| 约束 | 说明 |
| ---- | ---- |
| Lua 4.0 语法 | 整个 `lua/` 目录只支持 Lua 4.0，不能用 5.x 新语法（如 `goto`、位运算符、`table.unpack` 等） |
| 闭包限制 | **Lua 4.0 不支持闭包访问外层局部变量**（不能 access outer-scope variable）。写回调/嵌套函数时，外层变量必须通过参数传入或提升为全局变量 |
| `#!ra3luabridge` 前缀 | 每个 Lua 执行块必须以 `#!ra3luabridge` 开头，否则 RA3LuaBridge 不执行该代码。开发时 lua 文件带此前缀，同步进 `ScriptsData.json` 时由脚本自动保证 |
| 源码与地图脚本的关系 | `DSA_ggz/lua/` 里的全部 lua 是对 `ScriptsData.json` 的**拆分**；`ScriptsData.json` 是地图实际加载的脚本。改 lua 后需同步回 JSON（见 `analysis/sync_lua_to_json.py` 等工具） |
| 注释 | lua 文件里的注释是给开发者看的；同步进 ScriptsData.json 的只保留执行片段 |

### 5.0.1 自定义图片机制（huohuo.webp 示例）

地图支持插入**自定义图片**（webp），用于按钮图标、UI 图像等。参考实现位于 `lua/LUABOX/BUTTON/CenterTopBtnFunc.lua`（第 1150 行起，投降按钮图标）：

**核心 API 调用链：**

```
1. exTextureCreateFromBase64(base64字符串, "DXT5")
   → 返回 textureId（从 base64 数据创建纹理）

2. exPackedTextureImageCreate({
       Texture = textureId,          -- 上一步的纹理
       CoordsX = 0, CoordsY = 0,     -- 贴图在纹理中的位置
       DimensionsX = W, DimensionsY = H,        -- 显示尺寸（如 126x126）
       TextureDimensionsX = W, TextureDimensionsY = H,  -- 源纹理尺寸
       Rotated = 0
   })
   → 返回 imageId（可复用的图片 ID，存到全局变量）

3. 使用：
   - 顶部按钮：IconId = imageId（数字 ID），经 exCenterTopBtnShowForPlayer 显示
   - 其它 IconId 也可以用内置纹理名字符串（如 'Button_PlayerPower_FreeTrade'）
```

**base64 数据来源（已实测验证）：**
- 嵌入的 base64 解码后就是一张 **webp 图片文件**本身（缩略/缩放后的版本）
- 例：`huohuo.webp` 源文件 700x700，嵌入按钮的 base64 解码后是 **126x126** 的 webp（7722 字节）
- 所以流程是：**图片 → 缩放/居中裁剪 → webp → base64 → Lua 嵌入**

**一键转换工具：**

已提供脚本 `analysis/img_to_lua.py`（依赖 Pillow）：

```
python analysis/img_to_lua.py 图片.png --size 126 --var-name g_MyIconId --embed-name my_icon --output out.lua
```

- 自动居中裁剪为正方形并缩放到指定尺寸
- 输出可直接粘贴进 Lua 的完整代码（local 表 + 两条 ex 调用）
- 生成代码与 huohuo 例子的格式完全一致

**注意事项：**
- base64 用 Lua 长字符串 `[[ ... ]]` 包裹，可换行，勿用普通字符串
- `exTextureCreateFromBase64` 的格式参数固定传 `"DXT5"`
- 每个图片生成独立的 `embed-name`（local 表名），避免变量冲突
- 图片 ID 生成一次后存全局变量，可被多个按钮/UI 复用

### 5.0.2 海克斯符文选择界面素材（已找到）

**素材来源**：`E:\Ra3_AutoChess\海克斯符文 HextechRunes 其他游戏参考\本体\assets\images\`

| 目录 | 内容 | 说明 |
| ---- | ---- | ---- |
| `ui/` | 选项卡片**边框**（彩/金/银三档）、reroll/undo/remove 按钮及状态图 | 边框为 512x512 RGBA；当前项目在生成嵌入资源时为透明中心填充黑底 |
| `relics/` | 符文**图标**素材（数百个 png，120x120 / 256x256 不等） | 高对比度图形图标 |

> 素材只需边框图 + 图标即可拼出完整选项卡片，无需 cards/、powers/ 目录。

**三档边框图片**（与符文稀有度一一对应）：

| 稀有度 | 边框文件 | 尺寸 |
| ------ | -------- | ---- |
| 彩（棱彩） | `ui/augmentcard_frame_prismatic.png` | 512x512 |
| 金（黄金） | `ui/augmentcard_frame_gold.png` | 512x512 |
| 银（白银） | `ui/augmentcard_frame_silver.png` | 512x512 |

> 实际代码按概率抽稀有度，并非按科技阶段固定档位：第 3 回合彩/金/银为 5%/40%/55%，第 10、18 回合为 10%/45%/45%。

**完整选项卡片结构**（一个选项 = 一张卡片）：

```
┌───────────────────────────┐
│   （彩/金/银 边框图，全卡背景） │
│  ┌─────────────────────┐  │
│  │     图标区（上部）      │  │ ← 居中放置日冕原生图标
│  │      [原生图标]       │  │
│  └─────────────────────┘  │
│        标题区（中部）        │ ← 较大字号、较粗字体，居中
│       ═══ 符文名称 ═══      │
│      副标题（标题正下方）      │ ← 较小字号、颜色较浅，标注类型/稀有度
│   ─────────────────────   │
│        描述区（下部）        │ ← 最小字号、灰白偏色，左/居中对齐，行距适中
│   详细说明该选项的效果……      │
│                           │
│   （卡片底部留白/装饰线）      │
└───────────────────────────┘
```

- 项目通过 `analysis/hextech_frames_black.py` 将三张透明中心边框缩放为 128×128 纹理并填充卡框内部黑底后写入 `00_assets.lua`；保留素材原始方形画布与透明留白
- 三选一恢复实测满意的 200×200、间隔 30 横排布局；原生图标保留新版比例，缩小为 66×66 并置顶居中，标题位于图标下方居中
- 新文字定位算法不再依赖纯固定坐标：卡框与文字统一使用 `AlignX="left"` 屏幕锚点；按每行 UTF-8 字符估算宽度（中文等全宽字符权重 1、ASCII 权重 0.55、整体字体宽度系数 1.65），然后以 `卡框中心X - 估算文字宽度/2` 作为文字左边界。标题长度分档另用真实 UTF-8 字符数计算，并区分纯 ASCII 与中文的字面留白：四字标题事件卡/面板小卡分别向右校正 `22px/9px`；三字中文分别校正 `14px/5px`；三字纯 ASCII（当前为 `MCV`）分别校正 `8px/2px`；其他长度沿用默认 `14px/5px`。面板标题和玩家名继续使用算法中心值
- `$pNName` 由引擎显示时才展开，Lua 无法提前取得真实昵称宽度，当前按 3 个全宽字符估算居中；相比旧版锚点错位，长短昵称只会产生较小的剩余误差
- 海克斯总览面板恢复 50×50 小卡，图标按约三分之一比例显示；带兵种标题拆成“名称 + 换行 + 兵种”，避免超出边框
- 卡面只显示图标和标题，不直接铺开效果正文；卡框按钮和图标按钮的 `Desc` 均为“完整标题 + 效果描述”，聚焦任一区域都会显示日冕原生悬浮详情框
- 同名不同阶符文由卡框颜色表达阶级，标题不再追加“·银 / ·金 / ·彩”

**当前原生图标映射**（来源：`日冕地图lua例子/日冕图标ID表.txt`）：

| 符文 | 日冕原生图标 ID |
| ---- | --------------- |
| 大力 | `Button_PlayerPower_EmperorRage3` |
| 迅捷 | `Upgrade_CelestialSpeedUpdate` |
| 巡猎 | `SUA_Grinder_TurboBoost` |
| 万用瞄准镜（银/金/彩） | `AUA_Tank_TargetPainter`（原 `Button_CelestialAuxiliaryAimingSystem` 实测未正常显示） |
| MCV | `Button_JapanMCV` |
| 湮灭炸弹 | `Button_PlayerPower_Telekenetic` |
| 超级要塞 | `Button_JapanGigaFortressShip` |
| 五虎上将 | `CelestialCenturion` |
| 出口内销 | `Button_JapanAntiNavyShipTech3` |
| 高平两用 | `Button_VDVAntiAirVehicleTech1` |
| 先进火炮 | `Button_AlliedHarbingerGunship` |
| 安全感 | `Button_AlliedAegisLargeDefenseBase` |
| 启动资金 | `AUA_Bribe` |
| 固若金汤 | `Button_CelestialAntiVehicleInfantry_Skill` |
| 破烂王 | `Button_AlliedSalvageShip` |
| 无限火力 | `Button_SovietInterceptorAircraft` |
| 全频段阻塞干扰 | `Upgrade_AlliedAircraftCarrierDrone` |
| 神圣干预 | `Button_PlayerPower_IronCurtain` |
| 五雷天罚 | `Button_CelestialPantaOrbitalStrike` |
| 炽燃利息 | `Button_PlayerPower_ProductionKickback` |
| 磁暴突袭 | `Button_SovietTeslaAirAssault` |
| 塔防专家 | `Button_JapanPointShieldControlTower` |
| 海上霸主 | `Button_AlliedGaintAircraftCarrier` |

### 5.0.3 素材与 DSA 地图的接入方式

- 通用图片可经 `analysis/img_to_lua.py` 转为 base64；三档卡框当前由 `analysis/hextech_frames_black.py` 生成，关闭按钮由 `analysis/add_remove_btn_asset.py` 生成
- 当前已嵌入三档黑底卡框和普通关闭按钮；reroll/undo 与多状态关闭按钮尚未接入
- 首批符文已使用日冕原生图标，不需要额外嵌入图片资源；事件三选一与已拥有符文面板共用同一套图标映射
- 三选一使用 `exCreateCustomButtonForPlayer` + `exCreateCustomTextForPlayer` 组合卡框、原生图标和标题；详情由按钮 `Desc` 交给日冕原生悬浮详情框显示
- `relics/` 素材保留为未来替换或扩展图标时的可选资源，不是当前首批符文的运行依赖

---

## 6. 技术实现方案

### 6.1 新增文件

| 文件 | 职责 |
| ---- | ---- |
| `lua/GAME/HextechRune/00_assets.lua` | ✅ 已实现：三档卡框与关闭按钮图片资源嵌入（`g_HextechFrameGoldId` / `g_HextechFrameSilverId` / `g_HextechFramePrismaticId` / `g_HextechRemoveBtnId`） |
| `lua/GAME/HextechRune/01_rune_pool.lua` | ✅ 已创建：首批真实符文数据、玩家候选/持有状态、按玩家筛池、兵种标签、禁海过滤和无放回三抽 |
| `lua/GAME/HextechRune/02_event.lua` | ✅ 已实现：回合注册、全场稀有度、每玩家独立候选、真实选择/持有记录、屏幕三选一按钮和真实数据总览面板 |
| `lua/GAME/HextechRune/03_dialog.lua` | ⏳ 尚未创建：当前三选一自定义按钮 UI 直接实现在 `02_event.lua` 中，未使用独立对话框文件 |
| `lua/GAME/HextechRune/04_buff.lua` | ✅ 已创建首批效果：兵种过滤、伤害/攻速/移速/射程、启动资金、防御塔生命、回收倍率和飞机无限弹药；每回合为新单位补效果 |
| `lua/text/Localization.lua` | 🟡 部分实现：已有模式、稀有度、首批正式符文名称/描述、选择结果与面板文案；剩余符文文案待补齐 |

### 6.2 修改现有文件

| 文件 | 改动 |
| ---- | ---- |
| `lua/LUABOX/BUTTON/BtnChoiceDialogEventFunc.lua` | ✅ 已实现：抽卡模式选择（选项 4）、海克斯符文设置（选项 5，次数 0/1/2/3）、更多选项（选项 6）；海克斯默认值随入局前随机箱子配置联动，确认流程写入 `g_EnableHextechRune` |
| `lua/LUABOX/BUTTON/ButtonChoiceDialogManager.lua` | 无需改动（对话框直接复用） |
| `lua/huihe/spawn_0.lua` | 无需改动（事件自注册到 RoundLuaManager） |
| 抽卡/随机箱子设置处 | ✅ 已实现联动：未手动设置海克斯时，未开启随机箱子默认 `g_HextechCount = 0`，开启时默认 `g_HextechCount = 3`；房主确认配置后再根据次数写入 `g_EnableHextechRune` |

> **实测修复（2025-09）**：① 新插入脚本节点必须用 **CRLF 换行符**（RA3LuaBridge 需要），此前用 LF 导致脚本完全不执行——事件不触发、卡框图不显示的根本原因，`analysis/add_hextech_scripts.py` 已统一 `\r\n`；② 三选一展示改为**屏幕正中间 3 个方框**（`exCreateCustomButtonForPlayer` + `exCreateCustomTextForPlayer`，卡框素材 `g_HextechFrame*Id` 可调像素），不再用顶部按钮区/文字对话框。

### 6.3 事件触发流程

**当前项目实际流程（已实现）**：

```
HextechRune:OnRoundBegin(round)
  第 1 回合：测试阶段额外触发一次固定三选一（当前为究极生物、高平两用、湮灭炸弹）
  第 3/10/18 回合：
    if g_EnableHextechRune == 1 and IsFormalRound(round):
      RollFieldRarity(round)                 -- 全场统一抽出本次稀有度
      对每个仍有建筑的 Player_1..Player_6：
        若该玩家没有未完成选择：
          按已拥有的“基础符文 + 兵种”组合和禁海状态独立筛池
          从筛选结果中无放回抽取 3 个不同符文并创建卡框、重随按钮
        若该玩家仍有未完成选择：
          将本轮阶级和回合加入该玩家独立的先进先出队列
  玩家点击：
    记录真实符文和兵种标签
    立即执行一次选择效果
    隐藏并移除该玩家的 3 个按钮和文字
    刷新真实持有符文面板
    向该玩家显示选择结果
    若队列非空，立即按最新持有状态筛池并展示队首事件
```

**选择页与部署快捷键**：为 J、K 键分别注册独立热键命令。存在当前未完成的三选一时，按 J 可隐藏整个选择页，再按一次原样展开；隐藏期间显示“当前有未选择的海克斯，按'J'键展开面板”。玩家拥有湮灭炸弹部署次数时，J 键提示下方持续显示“按'K'键部署湮灭炸弹”，每按一次 K 消耗一次；次数归零后提示移除。热键注册与全局回调位于 `ExtraCommand.lua`。

**当前 buff 施加时机**：`LandArmySpawn` 完成生成与混编后会启用 `ShrinkMode_Trigger`，持续型海克斯与缩小模式在同一触发时机执行；海克斯按照具体单位类型和玩家 `UNITCOUNT` 配额切片后，为新战斗单位加载持久效果。选择发生在本回合中途时，仅为新选择的符文补处理该玩家的当前切片。即时经济/规则效果（启动资金、防御塔生命、破烂王）仍在选择时登记或执行。

### 6.4 关键 API / 约束（已在本图代码中验证）

| 能力 | API | 备注 |
| ---- | --- | ---- |
| 房主配置对话框 | `exShowCustomBtnChoiceDialogForPlayer` | 最多 10 参数 = `{玩家, dialogId, 标题}` + **7 个选项**；当前用于模式/次数设置 |
| 房主配置回调 | `onUserBtnChoiceDialogEvent` | 已统一转发到 `ButtonChoiceDialogManager:OnUserButtonChoiceDialogEvent` |
| 符文三选一原型 | `exCreateCustomButtonForPlayer` + `exCreateCustomTextForPlayer` | 正式事件使用屏幕中央自定义卡框按钮，不走文字选择对话框；第 1 回合固定测试三选一已在当前排查版本重新启用 |
| 回合钩子 | `RoundLuaManager.CallOnEveryRoundBegin(cb, args)` | 回合数 `exCounterGetByName("lvc")` |
| 单位增益 | `ObjectLoadAttributeModifier(unit, mod, 时长)` | 配合 `ObjectFindObjects` + `CreateObjectFilter` |
| 公共广播 | `exAddTextToPublicBoard` / `exAddTextToPublicBoardForPlayer` | — |
| 发钱 | `ExecuteAction('PLAYER_GIVE_MONEY', playerName, amount)` | — |
| 本地化 | `Localization.get("key")` | zh/en 双语，`on_language_changed` 自动刷新 |

### 6.5 已知技术要点（坑与对策）

1. **游戏模式对话框选项上限**
   现有 6 个选项 + 确认按钮 = 7 个选项，正好卡满 `exShowCustomBtnChoiceDialogForPlayer` 的 10 参数上限。直接加第 7 个选项会超限。
   **当前实现**：主菜单选项 4 进入抽卡模式子菜单，选项 5 进入海克斯符文次数子菜单，选项 6 进入更多选项；主对话框仍保持 6 个入口 + 1 个确认按钮。

2. **对话框按钮只能显示文字**
   房主配置对话框按钮无法展示富文本/多行描述；符文选择已改用自定义卡框按钮，可通过按钮 `Desc` 显示悬浮信息并配合自定义文字。
   **当前实现**：使用“紧凑竖向稀有度卡框 + 顶部居中日冕原生图标 + 下方居中标题 + 原生悬浮详情”。卡面不直接显示效果正文；事件图标按钮与卡框按钮执行同一选择。针对日冕文字实际视觉中心随字符数和字符类型变化的问题，三字中文事件卡/面板小卡分别右移 `14px/5px`，三字纯 ASCII（`MCV`）分别右移 `8px/2px`，四字标题分别右移 `22px/9px`。

3. **同轮无放回与跨轮不可重复名单分离**
   同一轮的候选从筛选池无放回抽取，因此三张不会是同一个完整符文。跨轮仅 `NonRepeatableRuneIds` 中的符文按持有状态排除；无兵种符文以 `runeId`、带兵种符文以 `runeId:unitType` 记录身份。当前名单为先进火炮、出口内销、高平两用、固若金汤、超凡邪恶、全频段阻塞干扰、神圣干预、炽燃利息、塔防专家、赌怪；超凡邪恶按兵种版本分别判重。湮灭炸弹允许重复取得，每份增加一次 K 键部署次数。`PlayerOwnedRunes[playerIndex]` 保存每次选择的完整实例，不同玩家状态相互独立。

4. **跨回合选择事件排队**
   每名真人玩家维护独立 `PlayerEventQueues`。如果第 10 或 18 回合到来时前一轮三选一仍未完成，本轮只记录全场已经抽定的阶级，不覆盖当前 `PlayerOptions` 和 UI；前一轮选完并移除按钮后立即按先进先出展示队首。候选延迟到真正展示时才按该玩家最新持有状态生成，因此前一轮刚取得的唯一符文会正确从后续池中排除。遭遇战电脑仍在事件回合直接随机获得符文，不进入队列。

5. **选择页显隐状态**
   `PlayerSelectionVisible` 只记录当前三选一是否显示。J 键隐藏时保留所有选择数据并显示顶部提示，再次按 J 恢复同一组选项；新事件首次展示以及排队事件接续展示时默认展开。热键通过 `ExtraCommand.lua` 的全局 `onUserHotKeyEvent` 转发，避免多个脚本重复定义全局回调相互覆盖。

6. **增益施加时机**
   进攻/防御类符文需对「本方对应兵种的单位」生效，且后续新造单位也要吃到增益。
   **对策**：战斗单位混编完成后，先按具体单位类型扫描阵营单位，再依次按三名玩家对应的 `UNITCOUNT` 数量切片，最后按「兵种标签」给各自切片施加 Buff。这样不依赖混编后的所有权，也不会把一名玩家的符文扩散给同阵营全部单位；「全体效果」符文单独标记，不受该分配规则限制。

7. **兵种独立候选与效果过滤**
   通用带兵种符文在候选池中展开为步兵 / 载具 / 飞机 / 海军四个独立项，即使玩家当前尚未生产对应兵种也允许选择，以覆盖之后生产或刷新的单位。个别符文可通过 `UnitTypes` 限定当前开放版本；无限火力目前只开放飞机。
   **对策**：用 `CreateObjectFilter` 按兵种施加效果；海军版本在禁海模式下从候选池中排除（`g_DisableSeaArmy == 1`）。

### 6.6 海克斯面板（顶部按钮 5，✅ 已实现）

**入口**：技能栏第 5 个按钮「海克斯面板」（图标 `Button_SovietChiefScientist`），**仅在启用海克斯符文时启用**（`g_EnableHextechRune == 1`）。地图加载时按钮 5 默认禁用，开局确认游戏模式后由 `CenterTopBtnFunc_UpdateHextechPanelButton()` 按配置刷新启用状态。

**交互**：
- 点击按钮 5 展开面板，再点一次收起（`HextechRune:TogglePanel`）
- 面板右上角自带图片关闭按钮（`exCreateCustomButtonForPlayer` + `g_HextechRemoveBtnId`，素材为 `hextechRemoveButton.png`）

**布局（横六筒造型，逻辑分辨率 1366x768，屏幕中央）**：

```
行1 (y=230): [上左名] [上中名] [上右名]   ← 上三玩家 = 天使 4/5/6 的玩家名
行2 (y=300): [符文区] [符文区] [符文区]    ← 上三玩家的真实持有符文（0～4 个）
行3 (y=420): [符文区] [符文区] [符文区]    ← 下三玩家的真实持有符文（0～4 个）
行4 (y=490): [下左名] [下中名] [下右名]    ← 下三玩家 = 恶魔 1/2/3 的玩家名
```

- 面板列基准 `PanelCenterX = 563`，三列中心 x = `563 + (列-2) * 240`（323 / 563 / 803）；标题单独固定在 `PanelTitleCenterX = 683`
- 每列内符文使用 50×50 小卡（`PanelRuneWidth=50` / `PanelRuneHeight=50` / `PanelRuneGap=10`），支持 0～4 个并按实际数量水平居中；顶部图标约为 16×16
- 玩家名用 `$pNName` 占位符（Content = `"$p"..index.."Name"`），计分板同款，引擎支持时显示真实昵称
- 面板已读取 `PlayerOwnedRunes` 真实持有列表，卡框按实际稀有度显示，内部显示原生图标和缩放标题；卡框及图标聚焦内容均为真实名称和描述。质变、赌怪属于即时奖励入口，本体不在面板中占槽；它们实际授予的奖励符文正常进入面板。面板显示上限保持 4 个。

**自定义元素 index 占用（新增）**：
- 符文按钮：`400 + (玩家i-1)*4 + 槽位`（实际为 401～424，每玩家按最大 4 槽预留）
- 关闭按钮：`500 + 玩家i`（501～506）
- 名字文字：`600 + 玩家i`（601～606）
- 标题文字：`610 + 玩家i`（611～616）
- 小卡图标按钮：`700 + (玩家i-1)*4 + 槽位`（701～724）
- 小卡标题文字：`800 + (玩家i-1)*4 + 槽位`（801～824）
- 事件卡图标按钮：`300 + (玩家i-1)*3 + 选项`（301～318）；事件卡框为 101～118，标题文字为 201～218
- 以上避开已用 index（1-7 顶部/升本、21-25、30+ RecycleUnit、999/1000 语言）

**实现文件**：`02_event.lua`（面板函数）+ `CenterTopBtnFunc.lua`（按钮 5 创建/刷新）+ `Localization.lua`（文案键）

---

## 7. 待办 / 后续研究

- [x] 创建 `01_rune_pool.lua` 与玩家正式符文状态，完成候选抽取、去重、兵种标签和禁海过滤
- [x] 将第 3/10/18 回合的空白同稀有度选项替换为真实符文，并记录玩家实际选择
- [x] 创建首批效果层 `04_buff.lua`，接入属性、资金、防御塔、回收倍率和无限弹药效果
- [x] 海克斯面板改为读取玩家真实已拥有符文，移除 `PanelTestRuneCount` 占位数据
- [x] 第 1 回合固定测试三选一已重新启用；当前固定展示究极生物、高平两用、湮灭炸弹
- [x] 扩展 `analysis/add_hextech_scripts.py`，按资源→符文池→效果→事件顺序及 CRLF 规则同步进 `ScriptsData.json`
- [x] 当前基础定义为银 9、金 13、彩 9；非禁海实际候选为银 24、彩 12、金色苏联 19/其他阵营 18，禁海时为银 19、彩 10、金色苏联 17/其他阵营 16
- [ ] 符文描述展示方式（自定义页面 / 已拥有符文列表）—— 用户确认留空待研究
- [x] 首批自定义卡片：彩/金/银边框 + 黑底 + 日冕原生图标 + 自适应标题 + 原生悬浮详情
- [x] 为首批已实现符文配置日冕原生图标，并在事件卡与总览面板中按比例复用
- [ ] 后续新增符文继续从日冕原生图标表挑选；只有没有合适原生图标时才考虑把 `relics/` PNG 转为嵌入资源
- [ ] 固若金汤实机确认：陆地与海上各强化第一座存活塔，每份按各塔基础最大生命线性 +25%；已记录 `T81～T84` 实测最大生命为 `2813/18000/55000/200000`，仍需确认海塔实测数值及引擎是否同步补充当前生命
- [x] 买二送一：已抽象为玩家级、随机自身阵营单位的回收计数效果；赠送直接写入单位池，不递归计数
- [ ] 符文数值平衡（第一版数值为占位，需实测调整）
- [ ] 掉线/中途加入玩家的符文状态处理
- [ ] 海克斯符文与「升本模式 / 死亡模式」等其它模式的叠加规则确认

---

## 8. 实现顺序建议

1. ~~模式开关：游戏模式二级对话框接入 + `g_EnableHextechRune` 标志 + 海克斯 UI 原型~~ ✅ **已完成并按当前项目验证**：
   - 游戏模式首页选项 5 为「海克斯符文设置」，进入配置子菜单：不启用 / 本场一个 / 本场二个 / 本场三个
   - 入局前未开启随机箱子时默认“不启用”；开启时默认“三个”；房主手动设置后不再被自动联动覆盖
   - `g_HextechCount`（0-3）/ `g_EnableHextechRune` / `g_HextechCountManuallySet` 变量
   - `02_event.lua` 当前启用第 1 回合固定测试事件，并实现 3/10/18 回合正式事件、三档稀有度概率、单次重随、自定义卡框点击与海克斯总览面板
   - `00_assets.lua` 已嵌入金/银/彩三张 128×128 黑底卡框和 76×46 关闭按钮
2. ~~创建 `01_rune_pool.lua`、真实候选/持有状态和无放回三抽~~ ✅ 已完成首批测试池
3. ~~将 `02_event.lua` 空白选项替换为真实符文，并让面板读取真实持有数据~~ ✅ 已完成
4. ~~创建 `04_buff.lua` 首批效果层并更新地图同步顺序~~ ✅ 已完成
5. 扩充每个阶级的符文池，保证不可重复名单逐步扩大后仍至少有 3 个候选
6. 实机验证首批效果：属性是否重复叠加、防御塔最大生命、回收倍率、飞机弹药和不同兵种过滤
7. Localization：补齐剩余正式符文名称、兵种标签、描述和公告文案（zh/en）
8. 符文图标：首批已完成日冕原生图标映射；后续新增符文继续补齐，必要时再使用参考 PNG 素材
9. 实现剩余生成、周期与规则修改类符文
10. 完整实机测试：覆盖 0/1/2/3 次、随机箱子联动、禁海、不同基础模式、玩家死亡/退出、未及时选择和多人同步
11. 数值平衡与正式发布清理
