# 海克斯符文系统设计方案（Hextech Rune System）

> 红警3日冕自走棋（DSA）自定义玩法设计文档
> 参考原型：英雄联盟「斗魂竞技场 / 海克斯大乱斗」三选一强化符文（Augment）肉鸽事件
> 状态：设计定稿，待实现

---

## 1. 背景与目标

借鉴英雄联盟斗魂竞技场的**三选一强化符文**肉鸽机制：每隔若干回合，每位玩家独立看到 3 个随机符文，选择 1 个获得，效果可持续整局、可叠加成长。本方案将该机制本地化到红警3日冕自走棋，形成全新的「海克斯符文」模式。

**核心目标：**

- 每名玩家在特定回合弹出**三选一**符文对话框，独立选择、互不影响
- 符文效果全部使用本图已有系统实现（AttributeModifier 增益、发钱、召唤等）
- 作为**新模式开关**接入现有游戏模式选择流程，房主可选开/关
- 符文池本地化设计，覆盖进攻 / 防御 / 经济 / 召唤 / 功能五大类

---

## 2. 核心机制总览

```
回合开始
  └─ RoundLuaManager._begin() 触发 HextechRune:OnRoundBegin(lvc)
       ├─ 模式开关已开启（房主选择 或 开启了随机箱子）？
       │    └─ 当前回合 ∈ 发放回合表（5/11/18，按次数 N 截取）？
       │         └─ 对每个存活玩家：
       │              ├─ 按稀有度概率抽 3 个符文（去重 + 每人独占 + 兵种标签）
       │              └─ 弹出三选一对话框（选项含兵种标签）
       └─ 玩家点击选择
            ├─ 记录符文 + 兵种到 g_PlayerHextechRunes[playerIndex]
            └─ 登记到回合回调 → 每回合出兵时按兵种对己方单位施加 buff
                 └─ 公共面板广播「玩家X获得了符文Y」
```

---

## 3. 触发时机（已确认）

本图科技自动解锁节奏（用户确认 + 脚本佐证）：

| 科技阶段 | 自动解锁回合 | 脚本触发条件（lvc） | 符文事件回合（解锁前一回合） |
| -------- | ------------ | ------------------- | ---------------------------- |
| T2（二本） | 第 6 回合 | NATURALUNLOCK__1：lvc ∈ [4,5] | **第 5 回合** |
| T3（三本） | 第 12 回合 | NATURALUNLOCK__2：lvc ∈ [4,11] | **第 11 回合** |
| T4（四本） | 第 19 回合 | NATURALUNLOCK__3：lvc ∈ [4,18] | **第 18 回合** |

- 每名玩家整局共经历 **N 次**三选一（默认 3 次，对应三个科技阶段）
- 事件在**备战阶段**弹出（RoundLuaManager 回合开始回调内），不影响战斗
- 玩家死亡/掉线时跳过该玩家（按存活判断）
- 一局的海克斯**发放次数可调**：房主开局选择次数（默认选 **3**），减少次数时**去掉后面的发放**：
  - 3 次 = 第 5、11、18 回合
  - 2 次 = 第 5、11 回合
  - 1 次 = 第 5 回合

---

## 4. 符文池设计（用户定稿）

### 4.0 发放节奏与稀有度概率

三档稀有度「彩 / 金 / 银」对应素材边框色（银 = `augmentcard_frame_silver`，金 = `augmentcard_frame_gold`，彩 = `augmentcard_frame_prismatic`，见 5.0.2）：

| 发放 | 回合 | 彩 / 金 / 银 概率 |
| ---- | ---- | ---------------- |
| 第 1 个海克斯 | 第 5 回合 | **5% / 30% / 65%**（银色提高、彩色降低） |
| 第 2 个海克斯 | 第 11 回合 | **10% / 40% / 50%**（默认基准） |
| 第 3 个海克斯 | 第 18 回合 | **10% / 40% / 50%**（默认基准） |

> 默认基准概率为 彩/金/银 = 10% / 40% / 50%；仅第 1 次（第 5 回合）银色概率提高至 65%、彩色降低至 5%。

**全局规则：**

- 每回合**出兵时**给予 buff（非仅选择时），海克斯默认**只作用于自己的单位**（个别"全体效果"符文除外）
- 符文带**「兵种」标签**：获得选项时会同时指定「步兵 / 载具 / 飞机 / 海军」之一，符文只对该兵种类型生效（禁海模式下不会出现"海军"标签）
- 一局的海克斯**发放次数可调**（默认 3 次；2 次 = 第 5、11 回合；1 次 = 第 5 回合；减少时去掉后面的发放）
- **启用方式联动**：与「抽卡功能 / 开启随机箱子」同理——若玩家在游戏前选择了**「开启随机箱子」**，则**一同视为开启海克斯系统**

### 4.1 银色符文

| 符文名 | 兵种 | 效果 | 本地化实现 |
| ------ | ---- | ---- | ---------- |
| 大力 | 兵种 | 伤害 buff | 伤害类 AttributeModifier |
| 迅捷 | 兵种 | 攻速 buff | `AttributeModifier_BoxRateOfFireUp` |
| 星界躯体 | 兵种 | 生命 buff，但伤害降低 10% | 血量类 + 伤害类组合 |
| 存护 | 兵种 | 防御 buff | 防御/护甲类 AttributeModifier |
| 巡猎 | 兵种 | 移速 buff | `AttributeMod_GenericSpeedSet200` 系 |
| MCV | — | 获得一个**非自身阵营**的 MCV | 生成单位 |
| 穿云定海 | — | 获得 1 个摇光 | 生成摇光单位 |

### 4.2 金色符文

| 符文名 | 兵种 | 效果 | 本地化实现 |
| ------ | ---- | ---- | ---------- |
| 买二送一 | — | 随机一个自身阵营单位**买二送一** | 复用现有造二送一机制（参考狂热武士） |
| 五雷轰顶 | — | **每 10 回合**获得一次五雷天罚 | 定时发放技能/天罚 |
| 固若金汤 | — | 防御塔加血（需确认 Lua 能否给建筑加血） | `NAMED_SET_MAX_HEALTH`（待验证） |
| 石油王 | — | 获得两个油井 | 生成油井建筑 |
| 海上霸主 | — | 获得一个大航母 | 生成航母单位 |
| 启动资金 | — | 获得 10000 资金 | `ExecuteAction('PLAYER_GIVE_MONEY', ...)` |

### 4.3 彩色（棱彩）符文

| 符文名 | 兵种 | 效果 | 本地化实现 |
| ------ | ---- | ---- | ---------- |
| 终极刷新 | — | 技能回合数 -1 | 技能冷却缩减 |
| 神圣干预 | — | 乙方全体单位和建筑周期性获得铁幕 | `AttributeModifier_IronCurtain` 定时施加 |
| 无限火力 | 飞机 | 获得无限弹药 | `ObjectSetWeaponSetUpdateWeaponCurrentAmmoCount` |
| 全频段阻塞干扰 | — | 敌方全体单位射程 -25% | 射程降低 AttributeModifier 作用于敌方 |
| 破烂王 | — | 回收倍率 +20% | 修改 `GetRecycleRate` 返回值 |
| 万用瞄准镜 | 兵种 | 射程 +35% / +25% / +15%（彩/金/银 三档可重复获取） | 射程类 AttributeModifier |
| 质变 | — | 随机一个**更高阶**的海克斯 | 重新抽取更高稀有度符文 |
| 赌怪 | — | 随机两个海克斯（不限阶级） | 额外抽 2 个符文 |

> 万用瞄准镜比较特殊：它是唯一一个**跨稀有度存在**的符文（彩/金/银 三档数值不同），可以重复获取叠加。

---

## 5. 技术约束与自定义图片机制

### 5.0 本图 Lua 开发约束（重要）

| 约束 | 说明 |
| ---- | ---- |
| Lua 4.0 语法 | 整个 `lua/` 目录只支持 Lua 4.0，不能用 5.x 新语法（如 `goto`、位运算符、`table.unpack` 等） |
| 闭包限制 | **Lua 4.0 不支持闭包访问外层局部变量**（不能 access outer-scope variable）。写回调/嵌套函数时，外层变量必须通过参数传入或提升为全局变量 |
| `#!ra3luabridge` 前缀 | 每个 Lua 执行块必须以 `#!ra3luabridge` 开头，否则 RA3LuaBridge 不执行该代码。开发时 lua 文件带此前缀，同步进 `ScriptsData.json` 时由脚本自动保证 |
| 源码与地图脚本的关系 | `DSA_ggz/lua/` 里的全部 lua 是对 `ScriptsData.json` 的**拆分**；`ScriptsData.json` 是地图实际加载的脚本。改 lua 后需同步回 JSON（见 `analysis/sync_lua_to_json.py` 等工具） |
| 注释 | lua 文件里的注释是给开发者看的；同步进 ScriptsData.json 的只保留执行片段 |

### 5.0.1 自定义图片机制（huohuo.webp 示例）

地图支持插入**自定义图片**（webp），用于按钮图标、UI 图像等。参考实现位于 `lua/LUABOX/BUTTON/CenterTopBtnFunc.lua`（第 1150 行起，投降按钮图标）：

**核心 API 调用链：**

```
1. exTextureCreateFromBase64(base64字符串, "DXT5")
   → 返回 textureId（从 base64 数据创建纹理）

2. exPackedTextureImageCreate({
       Texture = textureId,          -- 上一步的纹理
       CoordsX = 0, CoordsY = 0,     -- 贴图在纹理中的位置
       DimensionsX = W, DimensionsY = H,        -- 显示尺寸（如 126x126）
       TextureDimensionsX = W, TextureDimensionsY = H,  -- 源纹理尺寸
       Rotated = 0
   })
   → 返回 imageId（可复用的图片 ID，存到全局变量）

3. 使用：
   - 顶部按钮：IconId = imageId（数字 ID），经 exCenterTopBtnShowForPlayer 显示
   - 其它 IconId 也可以用内置纹理名字符串（如 'Button_PlayerPower_FreeTrade'）
```

**base64 数据来源（已实测验证）：**
- 嵌入的 base64 解码后就是一张 **webp 图片文件**本身（缩略/缩放后的版本）
- 例：`huohuo.webp` 源文件 700x700，嵌入按钮的 base64 解码后是 **126x126** 的 webp（7722 字节）
- 所以流程是：**图片 → 缩放/居中裁剪 → webp → base64 → Lua 嵌入**

**一键转换工具：**

已提供脚本 `analysis/img_to_lua.py`（依赖 Pillow）：

```
python analysis/img_to_lua.py 图片.png --size 126 --var-name g_MyIconId --embed-name my_icon --output out.lua
```

- 自动居中裁剪为正方形并缩放到指定尺寸
- 输出可直接粘贴进 Lua 的完整代码（local 表 + 两条 ex 调用）
- 生成代码与 huohuo 例子的格式完全一致

**注意事项：**
- base64 用 Lua 长字符串 `[[ ... ]]` 包裹，可换行，勿用普通字符串
- `exTextureCreateFromBase64` 的格式参数固定传 `"DXT5"`
- 每个图片生成独立的 `embed-name`（local 表名），避免变量冲突
- 图片 ID 生成一次后存全局变量，可被多个按钮/UI 复用

### 5.0.2 海克斯符文选择界面素材（已找到）

**素材来源**：`E:\Ra3_AutoChess\海克斯符文 HextechRunes\本体\assets\images\`

| 目录 | 内容 | 说明 |
| ---- | ---- | ---- |
| `ui/` | 选项卡片**边框**（彩/金/银三档）、reroll/undo/remove 按钮、纯黑背景框 | 边框为 512x512 RGBA |
| `relics/` | 符文**图标**素材（数百个 png，120x120 / 256x256 不等） | 高对比度图形图标 |

> 素材只需边框图 + 图标即可拼出完整选项卡片，无需 cards/、powers/ 目录。

**三档边框图片**（与符文稀有度一一对应）：

| 稀有度 | 边框文件 | 尺寸 |
| ------ | -------- | ---- |
| 彩（棱彩） | `ui/augmentcard_frame_prismatic.png` | 512x512 |
| 金（黄金） | `ui/augmentcard_frame_gold.png` | 512x512 |
| 银（白银） | `ui/augmentcard_frame_silver.png` | 512x512 |

> 对应关系：T2 事件（第 5 回合）出**银**卡为主 → T3 事件（第 11 回合）出**金**卡 → T4 事件（第 18 回合）出**彩**卡，与第 4 章符文池三档稀有度完全一致。

**完整选项卡片结构**（一个选项 = 一张卡片）：

```
┌───────────────────────────┐
│   （彩/金/银 边框图，全卡背景） │
│  ┌─────────────────────┐  │
│  │     图标区（上部）      │  │ ← 居中放置 relic 图标（高对比度）
│  │      [relic图标]      │  │
│  └─────────────────────┘  │
│        标题区（中部）        │ ← 较大字号、较粗字体，居中
│       ═══ 符文名称 ═══      │
│      副标题（标题正下方）      │ ← 较小字号、颜色较浅，标注类型/稀有度
│   ─────────────────────   │
│        描述区（下部）        │ ← 最小字号、灰白偏色，左/居中对齐，行距适中
│   详细说明该选项的效果……      │
│                           │
│   （卡片底部留白/装饰线）      │
└───────────────────────────┘
```

- 背景框为**纯黑色**（`ui/` 目录，配合边框图使用）
- 三张边框图 + 黑底即可拼出完整的选项卡片底

### 5.0.3 素材与 DSA 地图的接入方式

- 所有 png 素材需经 `analysis/img_to_lua.py` 转为 base64 嵌入 Lua（自动裁剪/缩放/转 webp），生成 `exTextureCreateFromBase64` + `exPackedTextureImageCreate` 代码
- 边框图（512x512）与图标图（120/256）建议**分别**生成独立的图片 ID，渲染卡片时叠加：底图（黑底+边框）在上、图标在下
- 三选一按钮当前是 `exShowCustomBtnChoiceDialogForPlayer` 的纯文字按钮，若要渲染完整卡片（边框+图标+标题+副标题+描述），需自定义页面（见「待办」研究项）
- 已拥有符文列表可复用同一套图标，缩小显示

---

## 6. 技术实现方案

### 6.1 新增文件

| 文件 | 职责 |
| ---- | ---- |
| `lua/GAME/HextechRune/01_rune_pool.lua` | 符文数据表（id、稀有度、兵种标签、名称key、描述key、buff登记函数） |
| `lua/GAME/HextechRune/02_event.lua` | 回合触发逻辑（挂 RoundLuaManager 回调，按次数 N 截取发放回合 5/11/18，随机箱子联动） |
| `lua/GAME/HextechRune/03_dialog.lua` | 三选一对话框（复用 ButtonChoiceDialogManager，选项带兵种标签） |
| `lua/GAME/HextechRune/04_buff.lua` | 每回合出兵时按兵种对己方单位施加 buff 的回调系统 |
| `lua/text/Localization.lua` | 追加符文名称 / 描述 / 公告文案（zh/en） |

### 6.2 修改现有文件

| 文件 | 改动 |
| ---- | ---- |
| `lua/LUABOX/BUTTON/BtnChoiceDialogEventFunc.lua` | 游戏模式选项新增「海克斯符文」入口（次数选择，默认 3）+ 确认流程写入 `g_EnableHextechRune` |
| `lua/LUABOX/BUTTON/ButtonChoiceDialogManager.lua` | 无需改动（对话框直接复用） |
| `lua/huihe/spawn_0.lua` | 无需改动（事件自注册到 RoundLuaManager） |
| 抽卡/随机箱子设置处 | 联动：玩家选了「开启随机箱子」（`g_DrawMode`/`g_LuckyCrateMode`）时，自动置 `g_EnableHextechRune = 1` |

### 6.3 事件触发流程

```
HextechRune:OnRoundBegin(round)
  if g_EnableHextechRune ~= 1 then return end   -- 房主选择 或 开启了随机箱子
  local tier = HextechRune:GetTierByRound(round, g_HextechCount)  -- 次数N截取发放回合
  --   N=3: 5→1, 11→2, 18→3 ; N=2: 5→1, 11→2 ; N=1: 5→1
  if tier == nil then return end
  for each playerIndex in 1..6:
    if PlayerAlive(playerIndex):
      local pool = HextechRune:PickThree(playerIndex, tier)  -- 按稀有度概率抽3个，带兵种标签
      --   第1次(第5回合): 彩/金/银 = 5%/30%/65%；第2、3次(第11、18回合): 10%/40%/50%
      ButtonChoiceDialogManager:ShowDialog({
        DialogId = HEXTECH_DIALOG_ID + playerIndex,
        PlayerName = "Player_"..playerIndex,
        Title = Localization.get("hextech.dialog.title", round),
        Choices = {符文名1(兵种), 符文名2(兵种), 符文名3(兵种)},
        OnChoice = function(self, btnIndex)
          local rune = pool[btnIndex]
          g_PlayerHextechRunes[playerIndex][rune.id] = true
          g_PlayerHextechRunes[playerIndex][rune.unitType] = true  -- 记录兵种
          HextechRune:RegisterBuffForRoundBegin(playerIndex, rune)  -- 每回合出兵时施加
          exAddTextToPublicBoard(...)   -- 广播
        end,
      })
```

**buff 施加时机**：符文选择后不立即对全图单位生效，而是登记到 `HextechRune` 的回合回调——**每回合出兵时**对"拥有该兵种标签的己方单位"施加对应 buff，保证新造单位也吃到加成。个别"全体效果"符文（如神圣干预、全频段阻塞干扰）不受兵种限制，作用于全部目标。

### 6.4 关键 API / 约束（已在本图代码中验证）

| 能力 | API | 备注 |
| ---- | --- | ---- |
| 三选一对话框 | `exShowCustomBtnChoiceDialogForPlayer` | 最多 10 参数 = `{玩家, dialogId, 标题}` + **7 个选项** |
| 对话框回调 | `onUserBtnChoiceDialogEvent` | 已统一转发到 `ButtonChoiceDialogManager:OnUserButtonChoiceDialogEvent` |
| 回合钩子 | `RoundLuaManager.CallOnEveryRoundBegin(cb, args)` | 回合数 `exCounterGetByName("lvc")` |
| 单位增益 | `ObjectLoadAttributeModifier(unit, mod, 时长)` | 配合 `ObjectFindObjects` + `CreateObjectFilter` |
| 公共广播 | `exAddTextToPublicBoard` / `exAddTextToPublicBoardForPlayer` | — |
| 发钱 | `ExecuteAction('PLAYER_GIVE_MONEY', playerName, amount)` | — |
| 本地化 | `Localization.get("key")` | zh/en 双语，`on_language_changed` 自动刷新 |

### 6.5 已知技术要点（坑与对策）

1. **游戏模式对话框选项上限**
   现有 6 个选项 + 确认按钮 = 7 个选项，正好卡满 `exShowCustomBtnChoiceDialogForPlayer` 的 10 参数上限。直接加第 7 个选项会超限。
   **对策**：仿照现有「抽卡模式选择」（选项 6 → 二级对话框 DRAW_MODE_DIALOG_ID）的做法，把「海克斯符文」做成**二级子对话框**（点进去开/关），主对话框结构不动。

2. **对话框按钮只能显示文字**
   按钮无法展示富文本/多行描述。
   **对策（待研究）**：第一版按钮直接显示「符文名」（短），选择后通过公共面板/消息区补充说明；后续研究自定义页面展示符文详情与当前已拥有符文列表（用户已确认此项留空待研究）。

3. **同玩家不可重复选同一符文**
   用 `g_PlayerHextechRunes[playerIndex][runeId]` 记录，抽卡时排除已拥有项；不同玩家各自独立，可同时拥有相同符文。

4. **增益施加时机**
   进攻/防御类符文需对「本方对应兵种的单位」生效，且后续新造单位也要吃到增益。
   **对策**：不在选择时一次性施加，而是登记后由 `HextechRune` 的**回合开始回调**在每回合出兵时按「兵种标签」过滤本方单位并施加 buff（参考现有 BUFF 系统写法）。这样新造单位自动吃到加成，且默认只作用于自己的单位；「全体效果」符文单独标记，不受兵种限制。

5. **兵种标签过滤**
   符文带「步兵 / 载具 / 飞机 / 海军」标签时，施加 buff 需按兵种过滤单位。
   **对策**：用 `CreateObjectFilter` 组合过滤（如 `IncludeThing` 列出该兵种的单位类型），或对每个单位判断模板名归属；海军标签在禁海模式下从抽取池中排除（`g_DisableSeaArmy == 1` 时过滤）。

---

## 7. 待办 / 后续研究

- [ ] 符文描述展示方式（自定义页面 / 已拥有符文列表）—— 用户确认留空待研究
- [ ] **自定义卡片页面**：用边框图（彩/金/银）+ 黑底 + relic 图标渲染完整三选一卡片（见 5.0.2 卡片结构）
- [ ] 符文专属图标（可用 `analysis/img_to_lua.py` 把 png 转 base64 嵌入，见 5.0.1）
- [ ] 为每个符文挑选 relic 图标素材
- [ ] 固若金汤：确认 Lua 能否给建筑加血（`NAMED_SET_MAX_HEALTH` 对建筑的可行性）
- [ ] 买二送一：复用现有造二送一机制的可行性确认
- [ ] 符文数值平衡（第一版数值为占位，需实测调整）
- [ ] 掉线/中途加入玩家的符文状态处理
- [ ] 海克斯符文与「升本模式 / 死亡模式」等其它模式的叠加规则确认

---

## 8. 实现顺序建议

1. `01_rune_pool.lua`：符文数据结构（id、稀有度、兵种标签、名称key、描述key、buff登记函数）
2. 模式开关：游戏模式二级对话框接入 + `g_EnableHextechRune` 标志 + 开局海克斯测试选项
3. `02_event.lua`：回合触发 + 抽卡逻辑（稀有度比例、次数可调、兵种抽取）
4. `03_dialog.lua`：三选一对话框 + 选择回调
5. `04_buff.lua`：每回合出兵时按兵种施加 buff 的回调系统
6. Localization 文案补齐（zh/en）
7. 符文图标：用 `analysis/img_to_lua.py` 生成 base64 嵌入，赋给按钮/页面
8. 实机测试与数值平衡
